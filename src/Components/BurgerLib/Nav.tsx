import { PrimaryNav, MenuLink, Menu, Hamburger } from "./Nav.style";

export const Nav = () => {
   return (
      <>
         <PrimaryNav>
            <Hamburger />
         </PrimaryNav>
      </>
   );
};
